# 功能設計文件：今日小記 / 閃念膠囊（Scratchpad & Intention Capture）

> 本文件記錄「今日小記」功能從構想到實作的完整設計討論與技術規劃。
> 最後更新：2026-07-11

---

## 💡 核心概念

將「隨手記錄」升級成**意圖捕捉器（Intention Capture）**。

使用者在草稿區輸入的任何靈感、臨時交辦事項、或當下閃念，可以在網頁上直接決定它的歸屬，再透過 API 發送至 Google Tasks 或 Google Calendar，讓閃念不再只是閃念，而是真正落地成行動或行程。

### 三段式工作流

```
使用者在草稿區輸入文字
      ↓
網頁判斷或使用者選擇：這是 Task？還是 Calendar Event？
      ↓
在網頁上完成細節填寫（如時間、到期日）
      ↓
呼叫 Google API 送出，資料落進 Google Tasks / Google Calendar
```

---

## ✅ 技術可行性評估

| 操作 | API 端點 | 難度 |
|---|---|---|
| 新增一筆 Google Task | `POST /tasks/v1/lists/{listId}/tasks` | ⭐ 簡單 |
| 指定 Task 到期日 | 同上，帶 `due` 欄位（RFC 3339 格式） | ⭐ 簡單 |
| 新增一筆 Google Calendar 事件 | `POST /calendar/v3/calendars/{calendarId}/events` | ⭐⭐ 中等 |
| 指定 Calendar 事件時間（含時區） | 同上，帶 `start` / `end`（含 `timeZone`） | ⭐⭐ 中等 |
| 選擇要加入哪個 TaskList / Calendar | 需在 UI 提供下拉選單供使用者選擇 | ⭐⭐ 中等 |

**結論：完全可行。** 我們已有登入授權框架，Google Tasks / Calendar API 均支援 `POST` 新增資料，直接呼叫即可。

---

## 🔑 設計決策討論

### 決策 1：轉換的觸發方式

**選項 A：使用者手動決定（✅ 建議優先採用）**

在草稿旁邊放兩個按鈕：`→ 加入 Tasks` 和 `→ 加入 Calendar`。

- ✅ 最直覺、最精準、最不會誤判
- ✅ 製作最簡單，邏輯最清晰
- ❌ 需要使用者多按一個按鈕

**選項 B：依照關鍵字自動判斷（🔵 未來加強版）**

- 含「明天」「下週五」「今天 3 點」等時間詞 → 自動建議轉為 Calendar
- 含「要記得」「提醒我」「買」「寄」等動詞 → 自動建議轉為 Task
- ✅ 體驗更智慧
- ❌ 中文時間詞解析複雜，容易誤判，需要大量規則或 NLP

> **結論**：第一版採用選項 A，選項 B 作為未來 AI 加強版預留。

---

### 決策 2：轉換時需要補充的資訊

**轉成 Google Task 所需欄位：**

| 欄位 | 說明 | 來源 |
|---|---|---|
| `title` | 任務名稱 | 自動從草稿文字帶入 |
| `due` | 到期日（選填） | 使用者選擇，預設為今天 |
| `notes` | 備註（選填） | 可把草稿全文帶入 |
| `listId` | 要加入哪個清單 | 下拉選單讓使用者選 |

**轉成 Google Calendar Event 所需欄位：**

| 欄位 | 說明 | 來源 |
|---|---|---|
| `summary` | 活動標題 | 自動從草稿文字帶入 |
| `start` + `end` | 開始與結束時間 | 使用者必填 |
| `calendarId` | 要加入哪個日曆 | 下拉選單讓使用者選 |
| `description` | 說明（選填） | 可把草稿全文帶入 |

> **核心挑戰**：如何在不破壞「隨手記錄」輕盈感的前提下，收集到這些必要資訊？

---

### 決策 3：轉換介面的 UX 設計方向

**思路 A：彈窗式（Modal）**

點擊「轉換」後跳出精緻的彈窗，填寫細節後送出。

- ✅ 資訊清晰，操作完整
- ❌ 會中斷「快速捕捉」的流暢感

**思路 B：Inline 展開式（✅ 建議優先採用）**

草稿輸入框旁常設兩個極小圖示（`📋 Task` / `📅 Event`）。點擊後，該行草稿**就地展開**一個極簡補充欄（只補填最必要的 1~2 個欄位），不跳離畫面。

- ✅ 最符合 Kontext 剋制美學
- ✅ 手感輕盈，不中斷思維流
- ✅ 展開的空間小，視覺干擾低

**思路 C：批次整理模式**

草稿區累積多條記錄，底部有「整理草稿」按鈕，一次性批次決定每條要轉成什麼。

- ✅ 適合習慣「先記再整理」的 GTD 使用者
- ❌ 介面相對複雜，對日系極簡風格有些超重

> **結論**：第一版採用思路 B（Inline 展開式）。

---

## ⚠️ 製作注意事項（Technical Gotchas）

### 1. 時區（TimeZone）問題
Google Calendar API 要求 `start.dateTime` 必須帶有正確的時區資訊（如 `+08:00`）。
- 可直接複用現有代碼中的 `getTimezoneOffset()` 邏輯。
- 建議統一封裝為 `getLocalRFC3339(date)` 的 helper function。

### 2. Google Tasks 不支援「時間」粒度的到期日
Google Tasks 的 `due` 欄位**只支援到「日」的粒度**（格式：`"2026-07-11T00:00:00.000Z"`）。
- 若使用者輸入的是「今天下午 3 點完成」，這在 Tasks API 層面無法精確表達。
- 建議 UI 上只提供「日期」選擇器，不提供「時間」選擇器，避免使用者誤解。
- 若需要「特定時間提醒」，應引導使用者使用 Calendar Event。

### 3. 選單資料需要已登入
TaskList / Calendar 的下拉選單需要使用者**已完成 Google 授權**才能列出。
- 未登入時，這個功能應整體隱藏或顯示「請先 SYNC 登入」的引導提示。
- 可直接複用現有的 `googleTaskLists` 和 `googleCalendarLists` 全域變數。

### 4. 送出後的草稿處理
成功送出 API 後，該條草稿應如何處理？

| 選項 | 說明 | 建議 |
|---|---|---|
| 自動刪除 | 最乾淨，最「無痕」 | ✅ 第一版採用 |
| 標記為「已轉換」並保留連結 | 可追溯，但畫面會累積 | 🔵 未來版本可選 |

### 5. API 配額
Google Calendar API 每天免費配額為 100 萬次請求，個人使用完全無需擔心。

---

## 🧩 MVP 規格（第一版最小可行設計）

### 草稿區 UI
- 一個多行文字框，支援每次輸入一條閃念
- 按 `Enter` 送出並存為一條草稿記錄（顯示在下方清單）
- 草稿清單存入 `localStorage`，重開瀏覽器後保留

### 每條草稿的操作列
```
[ 草稿文字內容 ]   📋   📅   🗑️
```
- `📋`：轉換為 Google Task
- `📅`：轉換為 Google Calendar Event
- `🗑️`：刪除此條草稿

### 點擊 📋 後展開（Task 轉換面板）
```
任務名稱：[ 草稿文字（可編輯）              ]
到期日：  [ 日期選擇器（預設：今天）        ]
加入清單：[ 下拉選單 ▼                      ]
                              [ 取消 ] [ 新增 ✓ ]
```

### 點擊 📅 後展開（Calendar Event 轉換面板）
```
活動標題：[ 草稿文字（可編輯）              ]
日期：    [ 日期選擇器                      ]
時間：    [ 開始時間 ] → [ 結束時間         ]
加入日曆：[ 下拉選單 ▼                      ]
                              [ 取消 ] [ 新增 ✓ ]
```

---

## 🗺️ 開發路線圖

| 階段 | 工作項目 | 狀態 |
|---|---|---|
| **Phase 1** | 草稿區 UI：輸入框 + 草稿清單 + localStorage 儲存 | ⬜ 待開始 |
| **Phase 1** | 每條草稿的刪除功能 | ⬜ 待開始 |
| **Phase 2** | Task 轉換面板：Inline 展開 + API 呼叫 | ⬜ 待開始 |
| **Phase 2** | Calendar Event 轉換面板：Inline 展開 + API 呼叫 | ⬜ 待開始 |
| **Phase 3** | 未登入時的引導提示 UI | ⬜ 待開始 |
| **Phase 3** | 送出成功後的動畫反饋（草稿消失動畫） | ⬜ 待開始 |
| **Future** | 關鍵字自動判斷（選項 B：AI 加強版） | 🔵 預留 |
| **Future** | 已轉換草稿的追蹤連結 | 🔵 預留 |

---

## 📎 相關文件

- [README.md](./README.md) — 專案總覽與功能說明
- [GOOGLE_API_INTEGRATION.md](./GOOGLE_API_INTEGRATION.md) — Google API 串接備忘錄
- [app.js](./app.js) — 主要應用程式邏輯（API 呼叫、渲染）

---

*本文件隨開發進展持續更新。*
